# ハピネス5 ／ みんなの水場

## このプロジェクトは何か

中小企業と、そこに関わる人たちのための組織健全化ツール「ハピネス5」の画面。
Google フォームで集めた5指標（Leadership・Communication・Mind・ES・SQ）と
「ありがとうカード」の投稿を、ひとつの水場として可視化する。

この画面は**ハピネス5のホーム画面**であり、記録のビューアではない。
投稿フォームへの導線がここにあり、メンバーは毎朝ここを開く。

### 呼び方

- 利用者は「社員」ではなく **「メンバー」** と呼ぶ。従業員でないコミュニティ参加者を含むため
- 画面の名前は **「みんなの水場」**

---

## 技術スタック

- Next.js（Pages Router）
- Google Sheets API（サービスアカウント、読み取りのみ）
- NextAuth.js（Google OAuth。External / Test モードで運用中）
- ホスティングは Vercel

---

## 変えてはいけない設計判断

**ここに書かれた判断は、良かれと思って覆さないこと。**
どれも組織づくりの思想（清水博の「場」と「与贈循環」）から導いた結論であり、
一般的なゲーミフィケーションの定石とは意図的にずれている。
変更が必要だと思ったら、実装する前に必ず人間に確認する。

### 1. 場はひとつしかない

個人の水場、個人のストリーク、個人バッジ、個人ランキングを**作らない**。
かつて「わたしの庭」と「みんなの庭」の2画面があったが、意図的に統合した。

理由：個人に報酬が返る設計は「労力を出したら自分に見返りが来る」という交換のモデルになる。
この画面が表現したいのは交換ではなく与贈。誰の投稿がどれだけ水場を育てたかは、
原理的に分解できない。分解できないことがこの設計の正しさ。

個人に依存してよいのは `viewer.todayPosted`（今日もう投稿したか）の1項目だけ。

### 2. 水場は干上がらない

活性度が落ちても、水位は下限（`LEVEL_FLOOR = 0.35`）で止まる。
枯れた表現、荒れ果てた表現、警告色を使わない。
冬・休眠・静けさとして描き、必ずホロホロチョウとハタオリドリは残る。

理由：投稿していない人への無言の圧力になり、場を閉じさせる。

### 3. 「人数が足りない」を不在理由として画面に出さない

生き物が来ていない理由をヒントとして表示するが、`why === "spread"`（参加人数不足）だけは
`absentHints()` で除外している。**この除外を外さないこと。**

理由：「ゾウは、あと3人投稿すれば来ます」という表示は、投稿していない人を数字で責める。
水位・天気の多様性・雨・時間帯の4つだけをヒントに使う。

### 4. 心のお天気に優劣をつけない

☀️晴れ・☁️曇り・☂雨・⚡疲れ気味に点数を割り当てない。
平均スコアの折れ線グラフを作らない（線が下がる＝悪い日、という読み方を生むため）。
推移を見せるなら100%積み上げの構成比。

むしろ**多様性が高いほうを健全と読む**。天気が3種類以上出ている週にだけ、
臆病な生き物（キリン・サギ・サイ・ヒョウ）が降りてくる仕掛けにしてある。
「⚡を出せる場は安全な場」という思想を、説教ではなく生態として表現している。

### 5. 与贈は、送り手と受け手を線で結ばない

ありがとうカードを画面上で「AさんからBさんへの矢印」として描かない。
いったん水場（＝場）に落ちて波紋が広がり、そのあとで土地が豊かになる、という順序で描く。

理由：直接つなぐと贈与＝返礼義務の図になる。場を経由することが与贈循環そのもの。

例外：`/report` の分析用ソシオグラムは直接の矢印で描いてよい。
日常の場は与贈的に、分析は構造として描く、という使い分け。
ただし**全体ソシオグラムは管理者限定**。孤立ノードを名指しで可視化しないこと。

### 6. 日誌には名前を出す

「水場の日誌」には投稿者の実名を出す。匿名にしない。決定済み。

### 7. 緑は累積で、減らない

水位・参加人数・天気の多様性は「いま」の状態なので変動する。
**緑だけは性質が違う。積み上がるだけで、一度育った緑は絶対に減らさない。**

一度水が通って森になった土地は、その年の作柄が悪くても森のまま、という考え方。
静かな週があっても、この土地がここまで来た歴史は消さない。
画面上も「この土地のいま」（変動する4指標）と「育った緑」（累積）を別枠にしてある。
この区別を崩さないこと。

| 与贈 | 緑への寄与 |
|---|---|
| マインド投稿 1件 | 1 |
| ありがとうカード 1件 | 3 |
| 歩数 100,000歩 | 1 |

7段階（裸地／まばらな草／草地／低木／アカシアが数本／林／水辺に葦、緑の帯）。
14人規模で1日およそ14ずつ積み上がり、最終段階まで約1年。
最初の1週間で目に見えて変わり、そこから先はゆっくりになる配分にしてある。
序盤で何も起きないと続かないため、意図的に前半を詰めている。

段階が上がると、草の密度と色、低木、アカシア、ナツメヤシ、水際の葦、
遠景の緑の帯が順に現れる。新しい植物が現れる瞬間が発見になるよう、
段階ごとに必ず「見たことのないもの」を1つ足すこと。

### 8. 歩数は合計だけを出す

歩数は健康データであり、扱いを誤ると人を追い込む。次を必ず守る。

- **合計だけを表示する。個人別の歩数を画面に出さない。ランキングは論外**
- **目標歩数を設定しない。「あと◯歩」を出さない**
- **未入力にペナルティを与えない。** 歩数を書かない人がいても緑は減らないし、
  他のメンバーの投稿で伸びる
- 表示してよいのは「みんなで歩いた距離（km）」の合計のみ

原則3（人数不足を不在理由に出さない）と同じ考え方。
個人の未達を可視化しない、という一貫した方針。

### 9. 乱数は必ずサーバー側で

生き物の頭数・配置を決める乱数は、日付をシードにして**サーバーで一度だけ**決める。
クライアントで `Math.random()` を回すと、同じ日でも人によって違う水場が表示され、
「ひとつの場」でなくなる。またリロードのたびに絵が変わると安っぽくなる。

同じ理由で、日替わりの演出はすべて「その日は何度開いても同じ」であること。

### 10. 人間を画面に描かない

水場のイラストに人間のキャラクターを出さない。
人間にできるのは水を足すことだけで、何が降りてくるかは操作できない、という関係を保つ。

まめ（仙台弁の犬）ときなこ（関西弁の猫）は例外だが、**おまけの位置づけ**。
小さく隅に置き、セリフは投稿を促すものにしない。
風向き・水の澄み具合・泥の足あと・アカシアの影といった、土地の話だけをさせる。

---

## データの在り処

Google スプレッドシート。**マインド・ありがとうカード・メンバー名簿は3つの別ファイルに分かれている。**
それぞれ `MIND_SHEET_ID` / `THANKS_SHEET_ID` / `ROSTER_SHEET_ID` で指定する
（`lib/sheets.ts` 参照）。同一のサービスアカウントに3ファイルすべての閲覧権限が必要。

タブ名は実際のシートで確認済み：マインドが「ハピネス」、ありがとうカードが
「フォームの回答 1」、メンバー名簿が「シート1」（`TAB_MIND` / `TAB_THANKS` / `TAB_ROSTER`
の既定値として `lib/sheets.ts` に反映済み）。

**列構成は下記を前提に実装しているが、一部未検証。
シートの実態と違っていれば `lib/sheets.ts` を直すこと。**

### マインド（タブ名「ハピネス」）
`A` タイムスタンプ / `B` 名前 / `C` 心のお天気 / `D` コメント / `E` 歩数

歩数の列は `E` と確認済み。`lib/sheets.ts` は `A:E` を読み、`lib/waterhole.ts` の
`parseSteps()` で数値化する。歩数は空欄・全角数字・「8,000」のようなカンマ区切り・
「約8000歩」などの表記揺れが混ざる可能性が高く、数値に変換できない行は 0 として扱い、
エラーにしない（未入力にペナルティを与えない、という原則8のため）。

### ありがとうカード（タブ名「フォームの回答 1」）
`A` タイムスタンプ / `B` あなたの名前 / `C` 送る相手の名前 /
`D` クレド / `E` 想い / `F` メッセージ

**`E` 列「想い」は画面に出さない。** 本人が自分のために書いた内心のメモという理解。
表示するのは `F` 列のみ。この扱いは人間に再確認すること。

### メンバー名簿（タブ名「シート1」）
`A` 氏名 / `B` メールアドレス / `C` 種別 / `D` ステータス

マインドシートにも、ありがとうカードシートにも**メールアドレスの列はない**。
ログイン中のメンバーを特定するには、必ずこの名簿で「メール → 氏名」を解決する。

### テストデータ

環境変数 `TEST_NAMES` にカンマ区切りで書いた名前は、全集計から除外する。

### 心のお天気の判定

選択肢の絵文字は異体字セレクタの有無が揺れるため、**絵文字で文字列比較しない**。
「晴」「曇」「雨」「疲」の一文字で判定する（`toWeatherKey()`）。
実データを一度流して、想定外の選択肢が `null` になっていないか確認すること。

---

## ファイル構成

```
lib/waterhole.ts        生き物の出現条件・場の指標の計算。純粋関数のみ。
                        Sheets にも Next にも依存させない（単体テスト可能に保つ）
lib/sheets.ts           Google Sheets の読み取りだけ。計算はしない。
                        列の位置が変わったらここだけ直す
pages/api/waterhole.ts  認証・キャッシュ・レスポンス組み立て
pages/index.tsx         みんなの水場（ホーム画面）
pages/report.tsx        レポート（未着手）
components/Waterhole/   水場のSVG、生き物21種、まめ・きなこ
```

責務の分離を崩さないこと。特に `lib/waterhole.ts` に Sheets や Next の import を
持ち込まないこと。

---

## いまの状態と、次にやること

### できているもの

- モックアップ（実データなし、`useMetrics` / `useFauna` / `ENTRIES` がハードコード）
- `lib/waterhole.ts` / `lib/sheets.ts` / `pages/api/waterhole.ts` の初版
  ※ **実際のシートに対して一度も動かしていない**

### 次にやること

1. 実際のシートに対して `/api/waterhole` を動かし、返ってくるJSONを確認する
2. **歩数の列を特定し、`lib/sheets.ts` の読み取り範囲を広げる。**
   あわせて `lib/waterhole.ts` に緑の累積計算（原則7）を実装する。
   累積は全期間が対象なので、直近7日・30日の窓を掛けないこと
3. モックの `useMetrics` / `useFauna` / `ENTRIES` を落とし、`/api/waterhole` を叩く
   `useWaterhole()` に差し替える
4. フォーム投稿は別タブで開き、タブに戻ってきた瞬間（`window` の `focus`）に再取得する
   ── 「投稿したのに水場が変わらない」というがっかりを防ぐため
5. Google フォームのプレフィルURL（`entry.XXXXXXX=氏名`）を名簿の氏名で組み立て、
   毎日プルダウンを選ぶ手間をなくす
6. `/report` の器を作る（期間指定 + 印刷CSS）

### レポートについて

集計の計算式とプロンプトは**すでに人間側で固まっている**。ロジックを新規に考えないこと。
作るのは器だけ。PDF出力は `@media print` でブラウザの「PDFに保存」に任せる
（html2canvas + jsPDF は日本語フォントの埋め込みで確実にハマる）。

レポートにも原則4・8は適用される。心のお天気を平均スコア化しないこと。
歩数は個人別に出さないこと（レポートは管理者が見るものだが、それでも出さない）。

---

## 環境変数（`.env.local`）

```
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_PRIVATE_KEY=          # Vercel では改行が \n のまま入るので復元が必要
MIND_SHEET_ID=               # ハピネス5（マインド）
THANKS_SHEET_ID=             # ありがとうカード
ROSTER_SHEET_ID=             # メンバー名簿
TAB_MIND=ハピネス
TAB_THANKS=フォームの回答 1
TAB_ROSTER=シート1
TEST_NAMES=
NEXTAUTH_SECRET=
NEXTAUTH_URL=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NEXT_PUBLIC_FORM_MIND=      # マインド投稿フォームのURL（「今日の一滴を落とす」）
NEXT_PUBLIC_FORM_THANKS=    # ありがとうカードフォームのURL（「ありがとうを贈る」）
```

**`.env.local` を絶対にコミットしない。** `.gitignore` に `.env*.local` があることを
毎回の作業前に確認すること。サービスアカウントの秘密鍵は一度履歴に入ると消せない。

---

## 作業の進め方

- 実行環境は Windows（PowerShell）。パス区切りやコマンドに注意
- 設計判断が必要になったら、実装する前に人間に確認する。
  このファイルの「変えてはいけない設計判断」に触れる変更は特に
- 実際のデータを確認してから設計を直す。推測で進めない
- アクセシビリティ：アニメーションは `prefers-reduced-motion` で止める
- SVGで回転させる要素には `transform-box: fill-box` を必ず指定する。
  既定の `view-box` 基準だと、図形自身ではなく画面全体を基準に回ってしまう
  （過去に犬のしっぽが体から外れる不具合を起こした）

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
